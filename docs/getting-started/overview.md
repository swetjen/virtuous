# Getting started

## Overview

Start with the RPC router and a small set of typed handlers. Serve docs and generated clients directly from your service.

## Start here

- `quickstart.md` for a minimal RPC server.
- `project-layout.md` for the recommended folder structure.
